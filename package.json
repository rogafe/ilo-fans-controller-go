{
  "name": "ilo-fans-controller-go",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_OPTIONS='--no-deprecation' pnpm run build:css:watch & NODE_OPTIONS='--no-deprecation' pnpm run build:js -- --watch",
    "build": "NODE_OPTIONS='--no-deprecation' pnpm run build:css && NODE_OPTIONS='--no-deprecation' pnpm run build:js",
    "build:css": "NODE_OPTIONS='--no-deprecation' pnpm dlx @tailwindcss/cli -i ./assets/src/styles.css -o ./assets/dist/styles.css",
    "build:css:watch": "NODE_OPTIONS='--no-deprecation' pnpm dlx @tailwindcss/cli -i ./assets/src/styles.css -o ./assets/dist/styles.css -w",
    "build:js": "NODE_OPTIONS='--no-deprecation' node build.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.34.5",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "alpinejs": "^3.14.9",
    "daisyui": "^5.0.28",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
